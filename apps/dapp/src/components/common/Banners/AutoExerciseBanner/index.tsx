import CtaBanner from 'components/UI/CtaBanner';

const AutoExerciseBanner = () => {
  return (
    <CtaBanner
      title="Auto Exercise is now available! 🚀"
      paragraph="Head over to the manage page to auto exercise your options!"
    />
  );
};

export default AutoExerciseBanner;
