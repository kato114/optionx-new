export type TokenData = {
  symbol: string;
  imgSrc: string;
};

export type Token = {
  address: string;
  chainId: number;
  symbol: string;
  name: string;
  logoURI: string;
  decimals: number;
};
